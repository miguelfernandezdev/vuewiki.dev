{
  "questions": [
    {
      "question": "How do you restrict a server/api/ file to only handle GET requests?",
      "options": [
        "Export a method property: export const method = 'GET'",
        "Add .get to the filename: users.get.ts",
        "Call setMethod(event, 'GET') inside the handler",
        "Use a routeRules: { method: 'GET' } config"
      ],
      "answer": 1
    },
    {
      "question": "Files placed in server/routes/ differ from server/api/ in what way?",
      "options": [
        "They only support GET requests",
        "They run before server middleware",
        "They expose routes without the /api prefix",
        "They are not auto-imported"
      ],
      "answer": 2
    },
    {
      "question": "Which Nitro utility throws a proper HTTP error from a server route?",
      "options": [
        "throw new HttpError(404)",
        "throw createError({ statusCode: 404 })",
        "return sendError(event, 404)",
        "setResponseStatus(event, 404)"
      ],
      "answer": 1
    }
  ]
}
